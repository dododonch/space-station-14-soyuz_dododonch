name: Publish

concurrency:
  group: publish
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      type:
        description: 'Тип паблиша'
        required: false
        default: 'base'
        type: choice
        options:
          - base
          - upstream
          - 'critical error'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install dependencies
      run: sudo apt-get install -y python3-paramiko python3-lxml python3-requests python3-yaml jq

    - uses: actions/checkout@v4.2.2
      with:
        submodules: 'recursive'

    # DS14-secrets-start
    - name: Setup secrets
      env:
        SSH_KEY: ${{ secrets.SECRETS_PRIVATE_KEY }}
      if: ${{ env.SSH_KEY != '' }}
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SECRETS_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        echo "HOST *" > ~/.ssh/config
        echo "StrictHostKeyChecking no" >> ~/.ssh/config
        echo "::group::Cloning secrets"
        git clone git@github.com:dead-space-server/space-station-14-secrets.git Secrets
        echo "::endgroup::"
        if [ ! -d "Secrets" ]; then
          echo "::error::Failed to clone secrets repository"
          exit 1
        fi
        echo "Secrets cloned OK"
        cp -R Secrets/Resources/Prototypes Resources/Prototypes/DeadSpaceSecrets
        cp -R Secrets/Resources/ServerPrototypes Resources/Prototypes/DeadSpaceSecretsServer
        cp -R Secrets/Resources/Locale Resources/Locale/ru-RU/deadspace-secrets
        cp -R Secrets/Resources/Textures Resources/Textures/DeadSpaceSecrets
        echo "Resources copied OK"

    - name: Verify secrets
      run: |
        if [ -d "Secrets" ]; then
          echo "Secrets: OK"
        else
          echo "::warning::Secrets not loaded - SECRETS_PRIVATE_KEY may not be configured"
        fi
    # DS14-secrets-end

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v4.1.0
      with:
        dotnet-version: 10.0.x

    - name: Install dependencies
      run: dotnet restore

    - name: Build Packaging
      run: dotnet build Content.Packaging --configuration Release --no-restore /m

    - name: Package server
      run: dotnet run --project Content.Packaging server --platform linux-x64

    - name: Package client
      run: dotnet run --project Content.Packaging client --no-wipe-release

    # DS14-Soyuz-start
    - name: Get build size
      id: build_size
      run: |
        TOTAL_SIZE=$(du -sb release 2>/dev/null | cut -f1 || echo "0")
        SIZE_HR=$(du -sh release 2>/dev/null | cut -f1 || echo "unknown")
        echo "size=$SIZE_HR" >> $GITHUB_OUTPUT
        echo "size_bytes=$TOTAL_SIZE" >> $GITHUB_OUTPUT
    # DS14-Soyuz-end

    - name: Publish version
      run: python3 -u Tools/publish_multi_request.py
      env:
        PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
        GITHUB_REPOSITORY: ${{ vars.GITHUB_REPOSITORY }}

    # DS14-Soyuz-start
    - name: Publish changelog (Soyuz Discord)
      continue-on-error: true
      run: python3 -u Tools/actions_changelogs_since_last_run.py
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        DISCORD_WEBHOOK_URL: ${{ secrets.CHANGELOG_DISCORD_WEBHOOK_DEADSPACE }}
    # DS14-Soyuz-end

    - name: Publish changelog (DeadSpace Discord)
      continue-on-error: true
      run: python3 -u Tools/actions_changelogs_since_last_run.py
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        DISCORD_WEBHOOK_URL: ${{ secrets.CHANGELOG_DISCORD_WEBHOOK }}

    # DS14-Soyuz webhook start
    - name: Get random announce
      id: announce
      run: |
        if [ -f "announce.txt" ]; then
          grep -v '^#' announce.txt | grep -v '^[[:space:]]*$' > /tmp/clean_announces.txt
          total_lines=$(wc -l < /tmp/clean_announces.txt)
          if [ $total_lines -gt 0 ]; then
            random_line=$(( (RANDOM % total_lines) + 1 ))
            announce=$(sed -n "${random_line}p" /tmp/clean_announces.txt)
            echo "announce=$announce" >> $GITHUB_OUTPUT
          else
            echo "announce=Нет доступных объявлений :(" >> $GITHUB_OUTPUT
          fi
        else
          echo "announce=Объявление не найдено :(" >> $GITHUB_OUTPUT
        fi

    - name: Set publish type messages
      id: publish_messages
      env:
        RANDOM_ANNOUNCE: ${{ steps.announce.outputs.announce }}
        BUILD_SIZE: ${{ steps.build_size.outputs.size }}
      run: |
        TYPE="${{ github.event.inputs.type }}"
        case "$TYPE" in
        "base")
            SOYUZ_MSG="$RANDOM_ANNOUNCE\n\n<:logo_UNION1:1504050698315235468>Сервер успешно обновлён!<:logo_UNION1:1504050698315235468>\n🥳Все последние изменения Союза ☭ в игре 🥳\n\n**📦 Информация о сборке:**\n• Размер сборки: \`$BUILD_SIZE\`"
            DEADSPACE_MSG="$RANDOM_ANNOUNCE\n\n<:logo_UNION1:1282351621375791218>Сервер успешно обновлён!<:logo_UNION1:1282351621375791218>\n🥳Все последние изменения Союза ☭ в игре 🥳\n\nА больше новых новостей разработки можно получать в дискорд сервере союза, ссылка в закрепах [чата союза](https://discord.com/channels/1030160796401016883/1487331042468565165)"
            ;;
        "upstream")
            SOYUZ_MSG="$RANDOM_ANNOUNCE\n\n<:logo_UNION1:1504050698315235468>Апстрим уже на сервере!<:logo_UNION1:1504050698315235468>\nНа сервере произошёл апстрим, все нововведения из основной сборки так же появились в сборке союза\n||<@&1504050876212445255>, <@&1486777697672888340> ||\n\n**📦 Информация о сборке:**\n• Размер сборки: \`$BUILD_SIZE\`"
            DEADSPACE_MSG="$RANDOM_ANNOUNCE\n\n<:logo_UNION1:1282351621375791218>Апстрим уже на сервере!<:logo_UNION1:1282351621375791218>\nНа сервере произошёл апстрим, все нововведения из основной сборки так же появились в сборке союза\n<@&1053952892786184232> \n\nА больше новых новостей разработки можно получать в дискорд сервере союза, ссылка в закрепах [чата союза](https://discord.com/channels/1030160796401016883/1487331042468565165)"
            ;;
        "critical error")
            SOYUZ_MSG="$RANDOM_ANNOUNCE\n\n<:logo_UNION1:1504050698315235468>Критическая ошибка сервера была исправлена!<:logo_UNION1:1504050698315235468>\nСервер обновлён, критическая ошибка была исправлена. Можно продолжать игру\n\n**📦 Информация о сборке:**\n• Размер сборки: \`$BUILD_SIZE\`"
            DEADSPACE_MSG="$RANDOM_ANNOUNCE\n\n<:logo_UNION1:1282351621375791218>Критическая ошибка сервера была исправлена!<:logo_UNION1:1282351621375791218>\nСервер обновлён, критическая ошибка была исправлена. Можно продолжать игру\n\nА больше новых новостей разработки можно получать в дискорд сервере союза, ссылка в закрепах [чата союза](https://discord.com/channels/1030160796401016883/1487331042468565165)"
            ;;
        *)
            SOYUZ_MSG="📦 Обновление"
            DEADSPACE_MSG="📦 Обновление"
            ;;
        esac
        echo "soyuz_message=$SOYUZ_MSG" >> $GITHUB_OUTPUT
        echo "deadspace_message=$DEADSPACE_MSG" >> $GITHUB_OUTPUT

    - name: Send Discord Webhook Notification (Soyuz Discord)
      if: success()
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.CHANGELOG_DISCORD_WEBHOOK }}
        PUBLISH_TYPE: ${{ steps.publish_messages.outputs.soyuz_message }}
      run: |
        CONTENT=$(printf "%b" "$PUBLISH_TYPE")
        
        jq -n \
        --arg content "$CONTENT" \
        '{
            username: "Обновления Союз-1 ☭",
            avatar_url: "https://github.com/PERed5/soyuz/blob/main/soyuz.png?raw=true",
            content: $content,
            embeds: [{
            image: {
                url: "https://media.discordapp.net/attachments/1303329439869833286/1390321152030806026/b6735b3e67e81887.gif"
            }
            }]
        }' | curl -H "Content-Type: application/json" -X POST -d @- $DISCORD_WEBHOOK_URL

    - name: Send Discord Webhook Notification (DeadSpace Discord)
      if: success()
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.CHANGELOG_DISCORD_WEBHOOK_DEADSPACE }}
        PUBLISH_TYPE: ${{ steps.publish_messages.outputs.deadspace_message }}
      run: |
        CONTENT=$(printf "%b" "$PUBLISH_TYPE")
        
        jq -n \
        --arg content "$CONTENT" \
        '{
            username: "Обновления Союз-1 ☭",
            avatar_url: "https://github.com/PERed5/soyuz/blob/main/soyuz.png?raw=true",
            content: $content,
            embeds: [{
            image: {
                url: "https://media.discordapp.net/attachments/1303329439869833286/1390321152030806026/b6735b3e67e81887.gif"
            }
            }]
        }' | curl -H "Content-Type: application/json" -X POST -d @- $DISCORD_WEBHOOK_URL
# DS14-Soyuz webhook end
